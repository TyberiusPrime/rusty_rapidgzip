
  - Property tests (proptest): (i) gzip(x) round-trips to x for arbitrary x,
  arbitrary member splits, arbitrary chunk sizes × thread counts — generalizing
  the existing split_invariant; (ii) arbitrary well-formed FASTQ reconstructs
  regardless of chunking/threads/member boundaries; (iii) malformed FASTQ yields
  the right error, never a panic.

  - Determinism as a standing invariant: assert byte-identical output across
  -P1/-PN and across chunk sizes (partially tested today — make it a property).

  - Differential oracle: you already have the safe vs intree inflate split and
  the AFL fuzz harness. Extend the fuzzer to also diff against system
  gunzip/flate2 on both valid and corrupted inputs, and run it in CI on a time
  budget.

  - Corruption/truncation suite: flip bytes in headers/trailers/body and
  truncate at every boundary; assert clean Err, never panic or hang. Codify the
  two safe_inflate truncation bugs from your fuzz notes as regression tests.

  - No-panic-on-input policy: audit unwrap/expect/panic!/indexing on
  input-derived data (the u32 overflow in A.1 is the prime example) and convert
  to errors. Run fuzzing under ASAN; run miri on the stringpod crate and the
  safe path.

  - [DONE] CI matrix: nix flake + GitHub workflow (.github/workflows/ci.yml).
  Full matrix via `nix flake check` (checks.{stable,clippy,fmt,msrv,corpus-full,
  miri}); lean subset for GitHub via `nix build .#test.<system>.<job>`.
    * MSRV is 1.85, not 1.80 — clap 4.6's tree uses edition 2024 (Cargo ≥1.85)
      and we use `#[expect(...)]` (≥1.81). The msrv job documents/enforces it.
    * -D warnings clippy: all pre-existing lints fixed; intentional index-based
      hot loops kept via `#[expect(clippy::needless_range_loop/...)]` w/ reasons.
    * both inflate engines: golden_hash now diffs fast vs safe against the
      recorded sums on every run (tests/golden_hash.rs).
    * thread counts 1 and N: already swept inside concat.rs / adversarial.rs.
    * RAPIDGZIP_FULL_CORPUS job present (checks.corpus-full).
    * synth-* fixtures committed (un-ignored) and REQUIRED (fail, not skip).

 the serial read path is slower than the parallel one without any real need, 
 just not optimized.

--
